export var primaryColor = '#00ccff';

export var suggestedList = [];

export const setPrimaryColor = color => {
    primaryColor = color
}

export const setList = items => {
    suggestedList = items
}